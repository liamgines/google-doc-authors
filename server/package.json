{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "app.ts",
  "scripts": {
    "start": "tsx --env-file .env ./app.ts",
    "startDatabase": "docker compose up --detach",
    "stopDatabase": "docker compose down",
    "seedDatabase": "tsx --env-file .env ./database/seed.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
